# 🧪 本地测试指南

---

## ⚠️ MongoDB 未安装

当前服务器未安装 MongoDB，需要安装后才能启动完整的后端测试。

---

## 📋 测试方案

### 方案 A：安装 MongoDB 后测试（推荐）

#### 1. 安装 MongoDB

**Ubuntu/Debian:**
```bash
# 导入 MongoDB GPG 密钥
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# 添加 MongoDB 仓库
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu $(lsb_release -cs)/mongodb-org/7.0" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# 更新并安装
sudo apt-get update
sudo apt-get install -y mongodb-org

# 启动 MongoDB
sudo systemctl start mongod
sudo systemctl enable mongod
```

**或使用 Docker:**
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

#### 2. 启动后端

```bash
cd /home/admin/openclaw/workspace/temp/自媒体 APP/backend

# 配置环境变量（已完成）
# .env 文件已创建

# 启动服务
npm run dev
```

✅ 成功后看到：
```
✅ MongoDB 连接成功
🚀 服务器运行在 http://localhost:3000
```

#### 3. 测试 API

```bash
# 健康检查
curl http://localhost:3000/health

# 获取文章列表
curl http://localhost:3000/api/articles

# 注册账号
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"123456"}'
```

#### 4. 启动前端

```bash
cd /home/admin/openclaw/workspace/temp/自媒体 APP/mobile-app

# 启动 Expo
npx expo start
```

使用 **Expo Go APP** 扫描二维码

---

### 方案 B：前端单独测试（无需后端）

前端页面可以单独测试，使用 Mock 数据：

```bash
cd /home/admin/openclaw/workspace/temp/自媒体 APP/mobile-app

# 启动 Expo
npx expo start
```

**可测试的功能**:
- ✅ UI 界面展示
- ✅ 页面导航
- ✅ 组件交互
- ❌ 真实数据加载（需要后端）

---

### 方案 C：使用 MongoDB Atlas（云端数据库）

1. 访问 https://www.mongodb.com/cloud/atlas
2. 创建免费集群
3. 获取连接字符串
4. 更新 `.env` 文件中的 `MONGODB_URI`
5. 重启后端

---

## 📊 已完成的测试

| 测试项 | 状态 |
|--------|------|
| 代码语法检查 | ✅ 通过 |
| 依赖安装 | ✅ 通过 |
| 路由加载 | ✅ 通过 |
| 模型定义 | ✅ 通过 |
| 资源导出 | ✅ 通过 |

---

## 🚀 快速启动命令

### 后端
```bash
cd /home/admin/openclaw/workspace/temp/自媒体 APP/backend
npm run dev
```

### 前端
```bash
cd /home/admin/openclaw/workspace/temp/自媒体 APP/mobile-app
npx expo start
```

---

## 📝 测试账号

```
邮箱：test@example.com
密码：123456
```

---

**下一步**: 安装 MongoDB 后启动完整测试，或直接进行 APK 构建
